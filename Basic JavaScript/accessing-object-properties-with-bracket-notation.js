// This script is about accessing object properties through bracket notation.
const testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

// Has to be used when the property is more than one word long.
const entreeValue = testObj["an entree"];
const drinkValue = testObj["the drink"];